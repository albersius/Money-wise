<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoneyWise" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.MoneyWise" parent="Base.Theme.MoneyWise" />

    <style name="App.Custom.ItemActiveIndicatorStyle" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/green</item>
    </style>

    <style name="App.Custom.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="itemIconSize">32dp</item>
        <item name="itemActiveIndicatorStyle">@style/App.Custom.ItemActiveIndicatorStyle</item>
    </style>

    <style name="App.Custom.TabIndicator" parent="android:Widget">
        <item name="tabSelectedTextColor">@color/green</item>
    </style>
</resources>